spring:
  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:postgresql:mediatheque-postgresql
    username: postgres
    password: postgres
  
  jackson:
    serialization:
      fail-on-empty-beans: false
      
